__pycache__/
*.pyc
.pytest_cache/
.venv/
.env
*.egg-info/
.ipynb_checkpoints/

benchmarks/scenarios/gen/

# Phase 3.1 ML — generated history + trained binaries (seeded, regenerable).
# Metadata JSON is committed as a result artifact; the model binary is not.
data/synthetic_load_history.jsonl
data/*.jsonl
ml/artifacts/*.joblib
