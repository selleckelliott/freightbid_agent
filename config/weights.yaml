scoring:
  profit_weight: 1.0
  rate_per_mile_weight: 50.0
  deadhead_miles_penalty: 0.5
  driver_hours_penalty: 5.0

bid_policy:
  target_margin: 0.20
  min_margin: 0.05
  max_margin: 0.35

# Ablation knobs for the profit-aware OR-Tools objective (Phase 2.2).
# Rates are derived from cost_model.yaml at load time; 1.0 = true cost model.
ortools_objective:
  deadhead_cost_multiplier: 1.0
  profit_multiplier: 1.0
